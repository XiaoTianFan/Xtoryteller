# Creating Themes

Themes are YAML files in `themes/`.

Recommended sections:

- `fonts`
- `colors`
- `typography`
- `spacing`
- `radii`
- `shadows`
- `borders`
- `motion`

Presentations select a theme with `theme: <slug>` and can add `themeOverrides` for targeted adjustments.
