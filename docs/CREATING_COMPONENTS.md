# Creating Components

Each component lives in `components/<name>/` and includes:

- `index.tsx`
- `manifest.yaml`
- `styles.module.css` when needed

Guidelines:

- Use semantic HTML.
- Reference CSS variables from the active theme.
- Keep props agent-readable in the manifest.
- Add a presentation example before relying on the component broadly.
