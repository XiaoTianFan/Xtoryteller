import path from 'node:path';

export const PROJECT_ROOT = process.cwd();
export const PRESENTATIONS_DIR = path.join(PROJECT_ROOT, 'presentations');
export const COMPONENTS_DIR = path.join(PROJECT_ROOT, 'components');
export const LAYOUTS_DIR = path.join(PROJECT_ROOT, 'layouts');
export const TRANSITIONS_DIR = path.join(PROJECT_ROOT, 'transitions');
export const BACKGROUNDS_DIR = path.join(PROJECT_ROOT, 'backgrounds');
export const THEMES_DIR = path.join(PROJECT_ROOT, 'themes');
export const SKILLS_REFERENCES_DIR = path.join(PROJECT_ROOT, 'skills', 'references');
