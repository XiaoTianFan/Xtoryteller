meta:
  title: Invalid Background Filter Opacity
  slug: invalid-background-filter-opacity
mode: stage
background:
  type: paper-shader
  presetRef: editorial-paper
  filter:
    mode: radial
    opacity: 1.2
steps:
  - layout: single-content
    components:
      - type: headline
        content: Invalid filter opacity
